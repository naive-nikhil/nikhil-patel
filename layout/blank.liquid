<!DOCTYPE html>
<html lang="en">
  <head>
    {{content_for_header}}
    <title>{{page_title}}</title>
    {{'main.css' |  asset_url |  stylesheet_tag}}
  </head>
  <body>
    {{content_for_layout}}
    {{'custom.js' |  asset_url |  script_tag}}
  </body>
</html>